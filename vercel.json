{
    "version": 2,
    "public": true,
    "cleanUrls": true,
    "buildCommand": "if [ ! -d \"flutter\" ]; then git clone https://github.com/flutter/flutter.git -b stable --depth 1; fi && export PATH=\"$PATH:$PWD/flutter/bin\" && flutter doctor -v && flutter pub get && flutter build web --release --web-renderer canvaskit",
    "outputDirectory": "build/web",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}